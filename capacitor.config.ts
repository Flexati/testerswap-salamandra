import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testerswap.salamandra',
  appName: 'testerswap-salamandra',
  webDir: 'dist/public'
};

export default config;
