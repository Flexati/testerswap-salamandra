import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.testerswap',
  appName: 'testerswap-salamandra',
  webDir: 'dist/public'
};

export default config;
